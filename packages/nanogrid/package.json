{
  "name": "@clisonix/nanogrid",
  "version": "0.1.0",
  "description": "Shared NanoGrid protocol bundle for reuse across Clisonix repositories",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "cbor": "^10.0.11"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
